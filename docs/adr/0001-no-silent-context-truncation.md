# 不做静默上下文截断

主流聊天客户端在上下文超限时静默截断历史或滑动窗口。Ramble 拒绝：生成请求永远发送 root → HEAD 的完整路径，超限时把 API 错误原样呈现给用户（零内容失败，不入树）。理由：对话树是上下文的唯一真相，静默裁剪会让模型行为不可解释，在调试系统提示词的核心场景下尤其致命；窗口不够是用户有权知道并自行决策的事实（开新分叉、显式压缩）。未来若做上下文压缩，必须是用户显式操作产生的有效内容（如摘要节点），绝不是请求时的隐形处理。

原则：不静默替用户做他们有权了解并做出的决定。
