name: ramble
description: "移动端 AI 对话客户端——git 式消息树"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  drift: ^2.27.0
  sqlite3_flutter_libs: ^0.6.0+eol
  shared_preferences: ^2.3.0
  flutter_secure_storage: ^10.3.1
  flutter_markdown: ^0.7.0
  intl: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.0
  freezed: ^3.0.0
  json_serializable: ^6.9.0
  drift_dev: ^2.27.0

flutter:
  uses-material-design: true
  generate: true
